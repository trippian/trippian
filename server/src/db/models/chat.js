import Promise from 'bluebird'
import db from '../db'

export default function () {
  create: (roomData, roomUserData, callback) => {
    return new Promise((resolve, rejecet) => {
      let cyper = ``
    })
  }
}